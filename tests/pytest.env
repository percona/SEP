# Deliberately empty. `[tool.pytest.ini_options] env` points ENV_FILE here so a
# pytest run reads no developer dotenv file, matching CI, where no .env exists.
# Settings the suite needs are pinned in that same `env` list; a value needed
# only for one run still works as an explicit `VAR=x pytest ...` prefix.
