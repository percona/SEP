; Consolidated SEP side-car: the five SEP processes plus the bundled Valkey
; broker under one PID 1. Runs as uid 1001 (sep); sock/pid under /tmp because
; the app dir is read-only to that user. All program output streams to the
; container stdout so `docker logs` is the single captured stream.

[supervisord]
nodaemon=true
logfile=/dev/stdout
logfile_maxbytes=0
pidfile=/tmp/supervisord.pid
loglevel=info

[unix_http_server]
file=/tmp/supervisor.sock
chmod=0700

[supervisorctl]
serverurl=unix:///tmp/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

; --- Broker (starts first) -------------------------------------------------
[program:valkey]
command=valkey-server --port 6379 --bind 127.0.0.1 --maxmemory 256mb --maxmemory-policy noeviction --save "" --appendonly no
priority=5
autorestart=true
startsecs=3
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

; --- Migration one-shots ----------------------------------------------------
; No depends_on in supervisord: each waits for Postgres, runs once, ends EXITED(0).
[program:migrate-sep]
command=sh -c 'until nc -z pmm-server 5432; do echo "[migrate-sep] waiting for postgres pmm-server:5432"; sleep 1; done; exec alembic --name=sep upgrade heads'
directory=/home/sep/app
priority=10
startsecs=0
autorestart=false
exitcodes=0
startretries=0
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:migrate-inventory]
command=sh -c 'until nc -z pmm-server 5432; do echo "[migrate-inventory] waiting for postgres pmm-server:5432"; sleep 1; done; exec alembic --name=inventory upgrade heads'
directory=/home/sep/app
priority=10
startsecs=0
autorestart=false
exitcodes=0
startretries=0
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:migrate-tasks]
command=sh -c 'until nc -z pmm-server 5432; do echo "[migrate-tasks] waiting for postgres pmm-server:5432"; sleep 1; done; exec alembic --name=tasks upgrade heads'
directory=/home/sep/app
priority=10
startsecs=0
autorestart=false
exitcodes=0
startretries=0
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

; --- API services, tolerate migration/DB lag via autorestart -----------------
[program:sep]
command=python -m app.sep.main
directory=/home/sep/app
priority=20
autorestart=true
startsecs=8
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:inventory]
command=python -m app.inventory.main
directory=/home/sep/app
priority=20
autorestart=true
startsecs=8
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:tasks]
command=python -m app.tasks.main
directory=/home/sep/app
priority=20
autorestart=true
startsecs=8
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

; --- Celery worker + beat ---------------------------------------------------
[program:celery-worker]
command=celery -A app.celery worker --loglevel=info
directory=/home/sep/app
priority=30
autorestart=true
startsecs=8
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true

[program:celery-beat]
command=celery -A app.celery beat --scheduler=sqlalchemy --loglevel=info
directory=/home/sep/app
priority=30
autorestart=true
startsecs=8
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
redirect_stderr=true
