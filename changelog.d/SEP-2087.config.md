Two optional Support diagnostics settings tune the run-outcome reconciliation sweep: `EXTENSIONS__ATW__RECONCILE_INTERVAL` (default every 10 minutes; `null` unregisters the sweep) and `EXTENSIONS__ATW__RECONCILE_BATCH_SIZE` (default 100). Both are defaulted, so an existing deployment needs no configuration change.
