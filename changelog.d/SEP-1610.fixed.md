The snippets list service-type filter now handles blank/absent service types and free-form values (including one literally equal to all) correctly, so the filter and its total always agree. The whole-dataset service-type facet and the list filter now trim service types through one shared definition, so a padded value (including tab/newline padding) is grouped identically on both paths.
