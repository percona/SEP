Inventory node and service writes now require a PMM origin (`external_id`, plus `source` for nodes), so a request that omits or nulls one is rejected with 422. Pre-existing rows without an origin are retired by the migration rather than deleted: they leave the active inventory views but stay resolvable through retired-inclusive reads.
