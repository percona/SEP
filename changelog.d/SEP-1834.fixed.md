A hot settings change no longer closes an API client out from under an in-flight task-log stream or file download, and a task-history file download no longer returns an empty payload, because its streamed request now carries the requesting user's credentials.
