XtraBackup: emit --safe-slave-backup when the Stop-replica-before-backup option is enabled, so physical backups succeed on multi-threaded replicas with GTID off (xtrabackup now owns the replica SQL-thread pause).
