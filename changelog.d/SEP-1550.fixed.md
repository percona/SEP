XtraBackup: the "Safe replica backup" option (renamed from "Stop replica before backup") now emits --safe-slave-backup, so physical backups succeed on multi-threaded replicas with GTID off (xtrabackup now owns the replica SQL-thread pause).
