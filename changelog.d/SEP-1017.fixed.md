HTTP 500 (`MissingGreenlet` / `DetachedInstanceError`) when dispatching, stopping, or syncing tasks on PostgreSQL/asyncpg — `schedule_annotation` now enforces its precondition and `_dispatch_queue_item`, `BaseExecutor.stop_task`, and `sync_queue_item` refresh the deferred `execution_request` column after `TaskHistoryManager.save` before reading it
