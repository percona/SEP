PostgreSQL Backups (backup_pg) now exposes a PUT /api/plugins/backup_pg/{task_name} route to update an existing backup task, derived from the shared app framework alongside the existing list, detail, create, execute, and delete routes
