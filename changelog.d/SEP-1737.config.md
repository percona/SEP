Add optional DATABASE.CONNECT_TIMEOUT (and the matching INVENTORY.DATABASE / TASKS.DATABASE keys) for PostgreSQL and MySQL engines; unset preserves each driver's own connect default, and the key is omitted for SQLite where it means lock wait rather than connect.
