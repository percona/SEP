Enforce the host system observation's at-least-one-fact rule in the database, storing an unset `installed_packages` or `config` as SQL NULL rather than as the JSON value `null` so the rule cannot be satisfied by a row carrying no observed fact; the upgrade rewrites existing JSON `null`s and then deletes any pre-existing fact-less observation row.
