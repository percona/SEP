Removed the auto-generated `/docs` and `/redoc` Swagger UI pages on the top-level FastAPI app; both now return `404`. Use the new `/api/docs` for the unified public API Swagger UI. The internal `/api/inventory/docs` and `/api/tasks/docs` sub-app UIs are unchanged.
