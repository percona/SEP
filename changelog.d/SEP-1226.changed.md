Accept legacy backup / backups values for plugin MODULE_NAME and remap them to mysql_backups with a deprecation warning. The legacy aliases will be removed in the next release; update settings.yaml overrides accordingly.
