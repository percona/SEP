Duplicate detection now recognises unique keys declared as table constraints, and treats a key column holding `0`, an empty string or `false` as filled rather than missing, so colliding updates return a conflict error instead of a generic bad request.
