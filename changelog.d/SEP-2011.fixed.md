A colliding update or insert now returns a conflict error instead of a generic bad request in every case the database rejects as a duplicate — including unique keys declared as table constraints, keys whose value is `0`, an empty string or `false`, and collisions created by another request at the same moment.
