Schema-driven plugin JSON APIs can now return a structured `connectivity_warning` field on task-creation responses, with an opt-in `check_connectivity` query flag (default `true`). The Checksums plugin (`POST /api/plugins/checksums/`) is the canonical demo of the convention.
