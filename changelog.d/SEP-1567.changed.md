React list pages (schema-driven plugins, Tasks, Snippets) now request `offset`/`limit` and show table pagination when the API returns a PaginatedResponse; schedule pages load every page so task joins are not capped at the first page.
