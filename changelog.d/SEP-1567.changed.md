React list pages (schema-driven plugins, Tasks, Snippets) now request `offset`/`limit` and show table pagination when the API returns a PaginatedResponse; schedule joins walk up to ~2500 task rows (`fetchAllPages`) so they are not capped at the first page.
