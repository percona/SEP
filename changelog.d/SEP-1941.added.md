The admin Settings connectivity check now probes the configured diagnostics-delivery receiver, so a wrong endpoint or credential is reported before a bundle is collected. The receiver's delivery plan must declare a GET-only probe request for it; one that does not is reported as such.
