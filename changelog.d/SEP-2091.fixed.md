The MongoDB log extractor, config file discovery and FTDC collection scripts no longer abort silently when mongod is not running. Each now walks its config-file and well-known-path fallbacks and, when nothing resolves, reports what it could not find; the log extractor and the FTDC collector also name the option that supplies it. Two MySQL backup check scripts likewise no longer abort when the backup log directory exists but cannot be read.
