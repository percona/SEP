Plugin authors now declare the task detail page section layout via the new PluginSchema.detail_view DSL field; the framework no longer infers Command/Args/Target from task.data shape. Rendered detail pages are unchanged for existing plugins.
