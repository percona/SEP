The base URL derived from a remote API client endpoint (Nomad, PMM, Grafana, Casdoor) no longer renders an embedded password in settings responses, YAML config exports, or the session-lifecycle and client-close log lines. Outbound requests are unaffected: the live value keeps the real credential.
