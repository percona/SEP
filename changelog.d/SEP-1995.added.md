Settings API responses now report who last saved each override and when; overrides saved before this release report no user, and their last-change time — or their creation time when they were never changed.
