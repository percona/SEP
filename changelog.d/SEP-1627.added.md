Auto-approve built-in snippets that match the shipped checksum manifest on sync (on by default), so a fresh install arrives with them ready to run. Existing deployments are affected too: the first sync after upgrading approves every built-in snippet whose digest matches, except those an administrator had explicitly un-approved. Turn the new setting off before that sync to keep approval fully manual.
