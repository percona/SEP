Unified Swagger UI for the PMM Extensions public API at `/api/docs` rendering a merged OpenAPI spec (core API + PMM Extensions web-app API) served from `/api/openapi.json`. The existing `/openapi.json` and `/api/extensions/openapi.json` documents are unchanged and remain the source of truth for the React frontend's `openapi-typescript` codegen.
