Add tasks settings INVENTORY_SYNC_INTERVAL (default unset — no inventory-sync schedule is seeded) and INVENTORY_SYNC_SYNCER (fully qualified "module.ClassName" syncer name, default unset — every configured syncer runs); the PMM-embedded profile sets them to 15 minutes and app.sep.sync.syncers.pmm.PMMSyncer.
