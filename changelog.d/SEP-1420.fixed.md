Snippet execution via the JSON API now honors the optional-sudo toggle; previously the sudo choice was silently ignored because the form's plain sudo input did not match the execution model's -sudo- validation alias
