DB-backed runtime overrides for SEP-side settings classes (SEPSettings, SnippetsSettings, MessagesSettings, the global Settings, AlertSettings, and app-owned classes) now take effect in the Celery worker without a restart, matching the API processes and the Tasks-side classes.
