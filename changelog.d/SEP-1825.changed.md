Settings admin API paths and responses identify a settings class by its Pydantic `__name__` (for example `AlertsSettings`) as a plain string rather than a closed OpenAPI enum, so app-owned classes can appear without regenerating the frontend client.
