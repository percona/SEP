The MySQL Backups create form now withholds its General, Encryption and Upload sections behind a "Show advanced options" control, so the required fields fit on one screen. Both the Backups and Restores forms nest each dependent option under the toggle it parameterises instead of listing it alongside, and field descriptions that were never shown on the service, host, schema, table and backup-source pickers are now visible.
