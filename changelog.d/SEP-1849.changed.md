A bearer-authenticated Grafana identity now reports the org role Grafana gave it (`none`, `viewer`, `editor`, `admin`, `super_admin`) instead of collapsing to `admin` or `viewer`, because the signed session assertion carries the role itself rather than only the admin flag. `isAdmin` is unchanged on every path, and no authorization decision changes yet.
