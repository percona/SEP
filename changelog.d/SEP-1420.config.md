Snippet execution via the JSON API now requires SNIPPETS_BASE_URL or BASE_URL to be set; the request-derived artifact-download URL fallback was removed so a misconfigured deployment fails loudly with a 400 instead of emitting an executor-unreachable URL
