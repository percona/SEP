The side-car now obtains its own Grafana service-account token when its container starts, so Grafana-backed sign-in and the PMM syncer work on a fresh install with no manual mint step. A deployment whose Grafana has admin-user bootstrap disabled still supplies a token itself; see `sidecar/README.md`.
