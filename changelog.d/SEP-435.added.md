MongoDB replica set diagnostics snippet (`mongodb_replica_set_collect.sh`) that collects `pt-summary`, server parameters, `getCmdLineOpts`, `serverStatus`, `hostInfo`, in-progress operations, `rs.status()`, `rs.conf()`, replication info and the latest oplog entry from a replica set node into a destination directory, then packs them into a tar.gz archive.
