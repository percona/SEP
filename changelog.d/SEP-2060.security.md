A task name is refused with HTTP 422 unless it is a single plain URL path segment, applied wherever SEP composes a task name into a request to the Tasks service, so a name carrying URL syntax can no longer reshape that request. A task group whose parent or child name would be refused is now rejected before the first task is created, rather than part-created and left behind by a rollback that cannot address it.
