A task name is refused with HTTP 422 unless it is a single plain URL path segment, applied wherever SEP composes a task name into a request to the Tasks service, so a name carrying URL syntax can no longer reshape that request.
