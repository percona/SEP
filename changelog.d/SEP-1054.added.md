Collect Diagnostic Data (ATW) in the React shell: sidebar entry opens `/atw` with cascading Category / Subcategory / Snippet selectors, per-snippet schema-driven execute form (including script preview), successful runs navigate to the snippet detail page; JSON API `GET /api/plugins/atw/` lists ATW-tagged snippets grouped by category with snippet schema, execute, and script-preview URLs, plus `GET /api/plugins/atw/schema` for plugin discovery
