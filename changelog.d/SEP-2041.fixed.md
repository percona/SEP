Resolve the XtraBackup payload defaults that could not work: the MySQL option-file path when HOME is unset, and a blank compression algorithm keyed on the backup type rather than the selected backup binary. An option file that cannot be read now fails the backup naming that file and the field that produced it, instead of surfacing as an authentication error.
