Resolve the XtraBackup payload defaults that could not work: the MySQL option-file path when HOME is unset, and a blank compression algorithm keyed on the backup type rather than the selected backup binary. An option file that cannot be read now fails the backup naming that file and the field that produced it, instead of surfacing as an authentication error. A compression algorithm the selected binary cannot run (`zstd` or `lz4` with `innobackupex` or `mariadb-backup`, `quicklz` with `xtrabackup`, `gzip` with any of them) now fails the backup naming the binary, where the backup-type-keyed check let it through to fail on the host; a blank or empty algorithm in a stored task resolves against the binary that will run it.
