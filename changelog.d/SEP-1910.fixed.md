An embedded deployment now dispatches its first PMM inventory sync when the container starts rather than one sync interval later; an install that already has an inventory-sync schedule keeps its current cadence.
