An embedded deployment now dispatches its first PMM inventory sync when the container starts rather than one sync interval later. Only a schedule created after the upgrade carries the marker, so an install that already has an inventory-sync schedule is unaffected: one that has dispatched before keeps its cadence, and one that has never dispatched still waits a full interval after every start, syncing only once the deployment stays up for a whole interval.
