Chained backup tasks run again after the parent finishes: PMM terminal annotations snapshot ``TaskHistory.execution_request`` via the ORM attribute ``loaded_value`` so ``sync_task_history`` no longer aborts with ``MissingGreenlet`` and chain dispatch runs (asyncpg/aiomysql)
