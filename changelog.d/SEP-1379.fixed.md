UI-triggered inventory sync no longer aborts with a 401 when the triggering user's session expires mid-run: it now authenticates service-to-service with the internal token, and the task poll loop tolerates transient auth/HTTP errors on a single status poll
