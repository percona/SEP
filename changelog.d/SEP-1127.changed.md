Collect Diagnostic Data (ATW): `GET /api/plugins/atw/` derives `category_root` from each snippet's `service_type` frontmatter (MySQL, MongoDB, PostgreSQL, ProxySQL, HAProxy, External, Generic) instead of a hardcoded MySQL root, enabling the Category dropdown when multiple service types are present; mysql-only corpora still auto-select a single MySQL root
