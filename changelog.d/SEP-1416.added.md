MySQL Restores is now a first-class registered app exposing a derived JSON API (list, detail, create, execute, update, delete, and `GET /schema`) under its own app key, where it was previously a Jinja-only sub-router with no JSON surface
