A custom auth provider (`AUTH__PROVIDER__CUSTOM__PROVIDER_CLASS`) must now set `role` (`app.core.auth.models.UserRole`) on every identity it constructs; `is_admin` is derived from the role and is no longer a settable field, so an unmigrated provider fails validation at authentication time.
