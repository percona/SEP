State-changing API requests (POST, PUT, PATCH, DELETE) now require an administrator. Non-admin users keep full read access; deployments whose non-admin users write through the API must grant them admin. Under Grafana-backed auth this makes Editors read-only alongside Viewers, since only the server-admin flag or an org Admin role maps to admin.
