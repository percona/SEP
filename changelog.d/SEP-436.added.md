MongoDB sharding status snippet (`mongodb_sharding_status.sh`) that runs `sh.status()` against a mongos router to capture the sharded-cluster topology — shards, databases, sharded collections, chunk distribution and balancer state — for support diagnostics.
