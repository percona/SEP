Restore request and task context fields (request_id, user, endpoint, task_id, task_name) on log lines. These key=value suffixes were dropped from every log line since v0.12.0 when the formatter stopped appending them.
