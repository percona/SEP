Creating a task history through the Tasks API now succeeds. The endpoint returned a server error on every call, so callers outside SEP could not record a run; SEP's own task execution never used it and was unaffected.
