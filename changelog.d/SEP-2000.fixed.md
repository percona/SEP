Creating a task history through the Tasks API now succeeds. The endpoint returned a server error on every call, so callers outside PMM Extensions could not record a run; PMM Extensions' own task execution never used it and was unaffected.
