PostgreSQL Backups now correctly triggers FULL backups per the configured incremental cycle. Previously, a config-key mismatch (`PG_*` instead of `PGBACKREST_*`) and a type mismatch in the weekday comparison meant a FULL backup could never run.
