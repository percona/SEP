MongoDB log components snippet (`mongodb_log_components.sh`) that captures `db.getLogComponents()` from a MongoDB instance — the current log verbosity level of every logging component (accessControl, command, query, replication, storage and the rest) — for support diagnostics.
