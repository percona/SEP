Inventory node and service detail pages now show a **System Facts** section displaying the system observation collected by the syncer — host OS version, installed packages, and config for nodes; database engine version for services — plus the `observed_at` provenance timestamp. Facts are read through a new read-only GET proxy on the inventory plugin gateway; pages that have no observation yet render a graceful "No system facts collected yet" empty state.
