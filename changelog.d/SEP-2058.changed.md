When a plugin schema declares only `item_display_name`, `item_display_name_plural` now defaults by pluralising that singular (Django-style English rules); omitting both still falls back to `display_name` so already-plural titles are not pluralised again. Explicit plurals remain for irregulars.
