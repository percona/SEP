MySQL Backups: fixed "Upload providers" multi-select field not appearing on the create form — the frontend type discriminator `'multichoice'` did not match the backend wire value `'multi_choice'`, causing the field renderer to silently drop the control
