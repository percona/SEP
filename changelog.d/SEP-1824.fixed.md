Persist Nomad task logs in full by holding the allocation until capture completes. Releasing the hold early needs the `namespace:alloc-lifecycle` capability; without it each allocation is instead retained until `log_capture_hold_seconds` elapses.
