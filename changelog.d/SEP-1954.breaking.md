Upgrading needs every Inventory API instance stopped before the inventory migration runs. A rolling upgrade that leaves the previous release recording sync health after the migration lets a late success clear a failing run that a newer failure opened; restart all Inventory instances together instead.
