Admin settings YAML download: `GET /api/sep/admin/settings/export` returns the merged effective SEP, Snippets, Messages, and Tasks settings (secrets redacted), with a Download YAML button on the Settings page; Tasks fan-out failure returns 502 with no partial export.
