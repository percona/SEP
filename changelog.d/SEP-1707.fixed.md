Deleting or patching a settings override now finds a row whose stored key differs only in casing, so a legacy mixed-case override is reverted or updated (with its stored key healed to the canonical spelling) instead of being left in place or duplicated.
