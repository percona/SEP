The MySQL restore form now stops an encryption format the named backup engine cannot produce while the operator is still filling it in, instead of only on submission: the served schema carries the engine/format pairings as rules the form evaluates itself. Opening a stored restore whose declaration hid an AES-256 key file now shows that key file, so saving the form keeps it.
