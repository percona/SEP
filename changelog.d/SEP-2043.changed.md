MySQL restore forms now reject an encryption format the named backup engine cannot produce (AES-256 outside XtraBackup) and require the AES-256 key file exactly when an AES-256 format is declared. A restore that set a key file without declaring AES-256 was accepted before and is rejected at submission now, even though the payload would have used that key file if the backup on disk turned out to be AES-encrypted.
