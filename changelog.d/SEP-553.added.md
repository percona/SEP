Task log viewer adds a line-cap dropdown (100 / 1000 / 5000 / All; default 1000) for finished task histories, backed by a new `tail` query parameter on the log stream API so large logs load without freezing the browser. The control is disabled while a task is running, since the live executor stream is uncapped.
