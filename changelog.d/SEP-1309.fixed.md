PostgreSQL Snippet Manager scripts now accept a configurable target database (dbname parameter, default postgres) and thread it into every psql call, so they connect successfully instead of failing when the connecting OS user has no same-named database.
