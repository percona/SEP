Added a finalize phase to om_bootstrap that enables MongoDB authorization once the first user exists, a POST /runs/{id}:cancel route to abort an in-flight run, and per-run configurable data/log paths, port and bind IP with per-member replica-set election settings (priority/votes/hidden/delay).
