Clarify MySQL backup encryption options with grouped labels, inline help, and validation blocking invalid combinations. In-place (`encrypt`) and post-run (`post_run_encrypt`) encryption are independent modes that each produce an encrypted backup; either one requires an encryption recipient.
