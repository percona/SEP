MongoDB "blocked writes" diagnostics snippet (`mongodb_blocked_writes_check.sh`) that periodically samples `pt-summary`, MongoDB internals (`serverStatus`, `currentOp`, `mongostat`) and OS metrics (`vmstat`, `iostat`, `mpstat`, `sar`, `top`) into a destination directory; stop early by creating an `exit-percona-monitor` marker file.
