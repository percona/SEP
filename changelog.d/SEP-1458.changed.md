Health report PDF download and ServiceNow upload now run as async jobs to avoid request timeouts. Rendered PDFs are staged on shared disk and reaped after a retention window, so only lightweight job metadata transits the result backend; an expired download returns HTTP 410.
