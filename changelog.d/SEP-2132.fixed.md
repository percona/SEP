The unified API docs page (`/api/docs`) no longer describes its document as the core API only; `/api/openapi.json` now states that it merges the core API with the PMM Extensions web app and names the Inventory and Tasks specs published separately.
