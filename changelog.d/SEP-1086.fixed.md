PMM annotations for backup-family tasks (MySQL/Mongo/Postgres backup, MySQL/Mongo restore, Archives) now target both the node and the service. Existing tasks created before this fix continue emitting node-only annotations until they are edited and saved through the UI.
