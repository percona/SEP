Migrate alters plugin to React via the schema-driven framework: JSON API at `/api/plugins/alters/` (list, create, detail, update, execute, delete for the parent execute task plus dry-run and pre-checks satellites), React UI at `/schema-change/alters` with declarative conditional rules (DSN table gate, inventory vs manual schema/table mutual exclusion), detail action bar with Pre-checks / Dry run / Execute, and create-time pre-checks→parent chaining via `ChainedPredecessor`. Add SEP proxy endpoints `GET /api/sep/services/{service_id}/schemas` and `GET /api/sep/schemas/{schema_id}/tables` so schema/table selectors no longer call inventory directly from the browser.
