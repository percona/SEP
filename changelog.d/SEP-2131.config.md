New AUTH__PROVIDER__GRAFANA__SERVICE_ACCOUNT_BEARER_REVOCATION_WINDOW (default 5 minutes; must not be negative, rejected at startup otherwise) is the revocation window for Grafana service-account bearers: how long a revoked, disabled or re-roled service account keeps its previous verdict on new requests. 0 re-verifies against Grafana on every request.
