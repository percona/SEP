MySQL Backups now serves a backup task's own catalogued runs at `GET /api/apps/mysql_backups/{task_name}/backups`, each run carrying a `backup_source` the restore form accepts as submitted alongside its backup type and host.
