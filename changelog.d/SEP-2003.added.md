MySQL Backups now serves a backup task's own catalogued runs at `GET /api/apps/mysql_backups/{task_name}/backups`, each run carrying its backup type, host, and a `backup_source` the restore form accepts as submitted — null when the run recorded no usable source.
