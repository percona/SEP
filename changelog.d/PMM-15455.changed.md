The MySQL Backups and MongoDB Backups schemas now label their restore tab "Restores" instead of "Restore", matching the restore page title; the label is served at runtime, so the React UI and an embedded PMM pick it up on the upgraded side-car with no frontend change.
