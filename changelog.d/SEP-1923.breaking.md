MySQL is no longer a supported SEP backing-store engine. Alembic cannot build the schema on MySQL 8 (SEP-1693), so no deployment ever finished starting on MySQL; this release removes the unreachable code paths instead. Product-side MySQL features (MySQLSyncer, mysql_backups, topology) are unaffected. The supported engines are now sqlite (development and test default) and postgresql (production).
