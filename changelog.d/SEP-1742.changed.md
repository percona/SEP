A credential-bearing URL that carries the redacted password display value (user:****@host) is now rejected at settings load from settings.yaml and the environment, so an installation that already has a masked endpoint pasted from a settings export fails at restart instead of starting and failing later on outbound requests; unchanged dashboard PATCH round-trips that restore the stored password are unaffected.
