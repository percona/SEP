Form-DSL bounds scan now flattens GroupedMetadata (Interval/Len) and descends into union members, so Interval/Len- and union-wrapped bounded fields (e.g. TcpPort | None, NonEmptyStr | None) expose their min/max in the derived form schema.
