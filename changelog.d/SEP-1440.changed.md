Add `make backfill-legacy-forms` one-time command to stamp `data['_form']` on legacy tasks for archives, checksums, backup_pg, mysql_backups, and restores so pre-migration rows become editable from the React UI. Fix `ServiceSelector` to hydrate scalar `service_id` values from stamped `_form` bodies into `name (type)` on edit forms.
