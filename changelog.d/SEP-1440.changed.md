Add `make backfill-legacy-forms` one-time command to stamp `data['_form']` on legacy tasks for archives, checksums, backup_pg, mysql_backups, and restores so pre-migration rows become editable from the React UI.
