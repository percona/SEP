The PMM-embedded side-car image's main SEP API no longer fails to start with a `ModuleNotFoundError` for an app package the restricted image strips; the settings-override callback registry is now derived from the activated app set. Deployments on the default full profile were never affected.
