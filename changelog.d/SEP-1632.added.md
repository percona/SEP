MongoDB (PBM) backups now emit Prometheus textfile-collector metrics (msp_backup_status, msp_backup_last_report_ts, msp_backup_enabled) so the StaleBackup / StaleBackupLog alerts fire for stalled Mongo backups
