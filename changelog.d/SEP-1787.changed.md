PostgreSQL Backups now validates the incremental cycle when a task is saved. The field is a dropdown of daily, weekly, and ISO weekdays (Monday through Sunday); values outside that set return 422 instead of failing later on the database host. Operators with a saved cycle such as monday must pick a valid option before the form will save.
