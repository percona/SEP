PMM alert-restore now distinguishes a genuine "not provisioned" 404 from an upstream gateway failure, so a proxy error no longer silently skips a contact point.
An upstream 502/503 now always answers with a JSON error, including on browser/session pages, instead of flashing a message and redirecting back.
