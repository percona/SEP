Authentication providers are now configured under `AUTH__PROVIDER__*` (Casdoor: `AUTH__PROVIDER__CASDOOR__*`); the legacy `CASDOOR__*` variables are deprecated but still honored, and the removed `AUTH_USER_MODEL` setting is replaced by provider selection.
