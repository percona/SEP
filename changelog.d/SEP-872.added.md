Node health probing with a Status column on the homepage Targets table, backed by a periodic Celery probe that registers a minimal per-host `raw_exec` batch job running `true` and purges it after completion; results persisted to a new `NodeHealthCheck` table and exposed through `GET /hosts/`
