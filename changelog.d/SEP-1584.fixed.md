MongoDB backups now reject an incomplete, malformed, or cross-wired per-task S3 storage config at create time (bucket/region required and well-formed, endpoint URL validated, filesystem/S3 fields no longer silently ignored) and apply the configured storage to PBM before each logical/physical backup, so backups land in the specified bucket instead of PBM cluster-wide storage
