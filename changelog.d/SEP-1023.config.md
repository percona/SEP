CONNECTIVITY_CHECK_DEFAULT flipped from True to False, so the Check connectivity checkbox on task creation forms is unchecked by default out-of-the-box. Operators who want the pre-checked behavior can still opt in by setting CONNECTIVITY_CHECK_DEFAULT: true in settings.yaml (or via the SEP__CONNECTIVITY_CHECK_DEFAULT=true environment variable)
