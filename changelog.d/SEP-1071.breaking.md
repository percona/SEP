`PluginSchema` JSON wire format flipped from camelCase to snake_case (`displayName` → `display_name`, `listView` → `list_view`, `serviceTypes` → `service_types`, `minLength` → `min_length`, etc.). The coordinated frontend update ships in the same release.
