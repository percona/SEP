Plugin task list pages no longer crash when the API returns a paginated envelope with a missing or `null` `items` field; the shared `unwrapTasks` helper now falls back to an empty list for malformed payloads.
