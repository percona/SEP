A task's detail page now follows a run's status to its end without a reload, and the Support diagnostics Results panel streams a running execution's log instead of reporting that none exists until the run finishes. The diagnostics run form also warns, without blocking submission, when the chosen host cannot run sudo for a selected script; it does so only for hosts whose sudo capability a host-facts sync has measured, so a host never measured shows no warning.
