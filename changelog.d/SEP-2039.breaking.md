A MySQL Backups create or update that sets a kill-queries option without 'Kill blocking queries', or a prepare-memory without 'Prepare for restore', is now rejected; the same applies to a MySQL Restore that sets a replication option without 'Slave from master'. Tasks saved earlier with those combinations still open in the edit form.
