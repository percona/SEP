New runtime-overridable settings govern tombstone collection: `SEP.INVENTORY.COLLECTION_INTERVAL` (default `null`, which seeds no schedule and collects nothing), `COLLECTION_RETENTION`, `COLLECTION_BATCH_SIZE` and `COLLECTION_MAX_BATCHES`.
