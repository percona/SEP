New runtime-overridable settings govern tombstone collection: `SEP.INVENTORY.COLLECTION_INTERVAL` (default `null`, which seeds no schedule and collects nothing; sub-minute periods are rejected, since the periodic-task write path could not then edit the schedule they seed), `COLLECTION_RETENTION`, `COLLECTION_BATCH_SIZE` and `COLLECTION_MAX_BATCHES`.
