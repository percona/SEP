New settings govern tombstone collection: `TASKS.INVENTORY_COLLECTION_INTERVAL` (default `null`, which seeds no schedule and collects nothing) plus `SEP.INVENTORY.COLLECTION_RETENTION`, `COLLECTION_BATCH_SIZE` and `COLLECTION_MAX_BATCHES`, all runtime-overridable.
