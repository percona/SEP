Plugin detail pages now show a "PII Anonymization" section listing the PII entity types (e.g. Email Address, IP Address) configured for anonymization on the task, gated on a new `pii_anonymization` schema capability flag. Checksums is the first plugin to opt in. The raw `anonymize_mask` bitmask is suppressed from the task information extras for all plugins.
