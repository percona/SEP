MySQL Backups rejects a GPG encryption timing no backup engine reaches without an upload provider, instead of accepting it and finishing a successful plaintext backup. Tasks already saved in that shape keep their stored form and still open in the edit form, but saving one now fails validation until an upload provider is selected or the unreachable timing is cleared.
