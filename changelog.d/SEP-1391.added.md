Dashboard Recent Tasks table shows user-meaningful task labels, hides system-internal rows (seeded maintenance tasks and system-initiated executor runs such as connectivity checks and syncs), and links each row to the owning app's task detail page
