Resolve a single-user lookup through the organization user listing when Grafana's service account is scoped to one organization instead of holding Server Admin, and report an unreadable Grafana user list as an upstream gateway error rather than an unhandled server error.
