MongoDB summary snippet (`mongodb_pt_summary.sh`) that runs `pt-mongodb-summary` against a MongoDB instance to produce a one-page diagnostic summary — build and host info, running operations, security, oplog, replica set and, for a sharded cluster, cluster-wide and per-shard balancer state.
