Archiver: reject archive jobs where the destination resolves to the same host, schema, and table as the source at payload-build time — catches cases Validator 1b could not detect (inventory ID resolves to same schema name, or whitespace-only host/schema falls back to source).
