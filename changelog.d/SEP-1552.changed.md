MySQL restore form uses clearer labels (Destination Database Service, Target database) and adds field descriptions for backup source and target database; myloader output is now streamed to the restore log.
