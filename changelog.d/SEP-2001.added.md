App schema fields can now declare a `destructive` consequence sentence naming what enabling them irreversibly destroys, published on the discovery schema for MySQL Restores' Overwrite tables and Archives' Delete Without Archiving. This ships the contract only; no form yet gates on it.
