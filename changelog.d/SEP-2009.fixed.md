The MySQL Restores create form no longer offers a Kill MySQL toggle on XtraBackup restores. The control never did anything: the restore always stops mysqld on the UI path and refuses to run when mysqld is up on the command-line path, regardless of how the toggle was set. Restore behaviour is unchanged.
