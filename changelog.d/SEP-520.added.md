MongoDB current operations snippet (`mongodb_current_op.sh`) that captures `db.currentOp(true)` from a MongoDB instance — every in-progress operation, including idle and system operations — with optional filtering by `--min-secs` (long-running operations only) and `--active-only` (drop idle operations).
