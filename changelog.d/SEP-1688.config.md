The PMM-embedded side-car image now ships a baked settings profile; per-deployment values are supplied as environment variables instead of a mounted settings.yaml. `SECRET_KEY` and `SEP_DB_PASSWORD` are required — see `sidecar/README.md` for the full contract.
