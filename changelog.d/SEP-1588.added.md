Publish a frontend-less consolidated SEP side-car image variant, tagged with a -sidecar suffix, that runs the SEP API, inventory, tasks, Celery worker and beat processes plus a bundled Valkey broker under a single supervisord
