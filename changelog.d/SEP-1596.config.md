HEALTH_REPORT.endpoint now preserves a configured path's trailing slash (only a bare origin's is trimmed). An intake that routes /path/ and /path differently previously received the slashless form, which could answer with a redirect that replayed the upload body — credentials included — over plaintext
