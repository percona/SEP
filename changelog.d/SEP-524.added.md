MongoDB profiling status snippet (`mongodb_profiling_status.sh`) that captures `db.getProfilingStatus()` from a MongoDB instance — the database profiler level, slowms threshold, sample rate and filter — for every database, or for a single database with `--database`, for support diagnostics.
