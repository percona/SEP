Periodic-task responses now carry a `timezone` field stating the zone the schedule is defined in, and `next_runs`, the next three run times. A new `POST /api/sep/periodic-tasks/schedule/preview/` endpoint reports both for a schedule that has not been saved.
