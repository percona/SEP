Add the log_capture_hold_seconds Nomad executor setting, defaulting to 90 seconds. It must outlast at least two log-sync cadences, or a step shorter than the cadence is collected before any sync samples it.
