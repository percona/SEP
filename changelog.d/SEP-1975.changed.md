The PMM side-car now mints an ENCRYPTION_KEY when neither an environment variable nor a SECRETS_DIR file supplies one, persisting it under the state directory; mount a volume at /home/sep/state so it survives a container recreate. Minting is refused, and the container stops, when any of the three service databases already holds encrypted override values or cannot be reached, because a newly minted key could not decrypt them.
