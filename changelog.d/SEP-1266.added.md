Schema-driven React UI for PostgreSQL backups at `/backups/postgresql`, replacing the placeholder page. The shell mounts it against the backup_pg JSON surface introduced in SEP-1063 through the shared schema-driven app framework and supports list, create, detail, and delete flows.
