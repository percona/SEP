Schema-driven React UI for PostgreSQL backups at `/backups/postgresql` (`@sep/plugin-backup-pg`), replacing the placeholder page. The plugin mounts against the `/api/plugins/backup_pg/` JSON surface introduced in SEP-1063 and supports list, create, detail, and delete flows via the shared `SchemaDrivenPlugin` framework.
