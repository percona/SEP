Add `SEP.API_READINESS_TIMEOUT` and `SEP.API_READINESS_POLL_INTERVAL`, which tune how long Celery beat waits for the HTTP API to answer its health probe before starting ungated. Raise the timeout on a deployment whose application startup runs long.
