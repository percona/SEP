Renamed the SEP.PLUGINS config key to SEP.APPS. The legacy PLUGINS key (settings.yaml and the SEP__PLUGINS env var) still loads with a startup deprecation warning, and APPS takes precedence when both are set. The settings-export output key and Class.KEY selector also change from SEP.PLUGINS to SEP.APPS.
