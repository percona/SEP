Renamed the EXTENSIONS.PLUGINS config key to EXTENSIONS.APPS. The legacy PLUGINS key (settings.yaml and the EXTENSIONS__PLUGINS env var) still loads with a startup deprecation warning, and APPS takes precedence when both are set. The settings-export output key and Class.KEY selector also change from EXTENSIONS.PLUGINS to EXTENSIONS.APPS.
