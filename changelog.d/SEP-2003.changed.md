The MySQL Backups per-service catalog endpoint `GET /api/apps/mysql_backups/services/{service_id}/backups` now returns a `backup_source` on each run, matching the task-scoped view of the same run; it is null when the run recorded no source the restore form accepts.
