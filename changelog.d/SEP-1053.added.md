React-based UI for the snippets plugin via the shared schema framework, plus the `ScriptPreviewField` framework field type and the `Deprecation` HTTP header convention for legacy Jinja2 plugin routes
