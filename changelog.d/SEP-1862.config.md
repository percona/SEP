The PMM-embedded profile no longer runs SystemFactsSyncer, so system-facts collection is off by default in that build. Its observations were read only by the removed inventory browser page. Nothing else changes: the syncer code, the observation models, the inventory service's system-observation endpoints and every already-collected row are untouched, and a SEPSettings.SYNCERS override restores collection with no code change.
