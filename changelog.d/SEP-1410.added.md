Admin-only page at /admin/apps to enable or disable apps at runtime, with each app's live lifecycle state, automatic refresh while a change is in progress, a lock on protected apps, and a force-disable action for stalled drains
