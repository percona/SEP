List filtering on disk-backed pages now treats a search term with leading or trailing spaces the same way the database-backed pages do, so a padded term no longer matches rows it would otherwise exclude.
List filtering on disk-backed pages with no searchable columns now returns every row for a search term, matching the database-backed pages, instead of returning nothing.
