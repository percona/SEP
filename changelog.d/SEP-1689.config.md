New `SETTINGS_OVERRIDE_ALLOWED_KEYS` setting restricts which settings the override API may write, as a default-locked allowlist of `ClassName.KEY` entries; unset (the default) leaves every deployment unrestricted.
