Snippet raw-file download: authenticated `GET /api/plugins/snippets/{snippet_filename}/download` streams the on-disk source (full script plus YAML frontmatter) as an attachment, and the snippet detail page adds a download control that saves the file via the same API.
