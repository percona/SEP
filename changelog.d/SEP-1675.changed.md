Empty YAML list overlays now clear inherited list values during recursive dict merge — settings profile overlays, `LOGGING_CONFIG`, and syncer extra kwargs (previously prepended and left the base list unchanged).
