Invalid or stale `messages` cookies no longer cause an HTTP 500 loop — they are treated as empty and cleared. Stale-session 404/500 errors now redirect to login and clear the stale session cookie instead of wedging the browser.
