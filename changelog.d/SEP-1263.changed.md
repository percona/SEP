MongoDB restore task payloads are built from typed per-leg models and a shared factory (`RestoreTaskGroupPayloads`), replacing four duplicate `_build_*` helpers; restore and API behavior are unchanged.
