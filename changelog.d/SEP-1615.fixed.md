Settings PATCH now persists real secret values instead of the redaction mask, and keeps the stored secret when a masked value is resubmitted (including subclass secrets in polymorphic collections such as ``AlertSettings.PROVIDERS``)
Secrets saved through the settings API before this release were stored as the redaction mask and must be re-entered
