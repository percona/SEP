Settings PATCH no longer replaces stored secrets when the client resubmits the masked value, including subclass secrets inside polymorphic collections such as ``AlertSettings.PROVIDERS``. Secrets already persisted as the ten-asterisk mask through earlier builds stay corrupted and must be re-entered; a masked resubmit now preserves them, so the API cannot tell those rows apart from healthy secrets
