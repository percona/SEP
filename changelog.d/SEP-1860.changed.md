The PMM-embedded deployment now refreshes its inventory cache from PMM every 15 minutes with no operator action; an install that deliberately left the sync unscheduled begins syncing on upgrade unless TASKS.INVENTORY_SYNC_INTERVAL is set to null, and an install that already attached its own inventory-sync interval keeps exactly that interval.
