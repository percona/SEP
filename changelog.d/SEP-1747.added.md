Publish an app-restricted PMM-embedded image variant, tagged with an `-embedded` suffix, containing only the app packages the embedded settings profile activates. It targets fresh deployments only — a database already migrated by an unrestricted image carries migration branches this image does not ship — and its activation list can only be narrowed, never widened, since activating an app the image omits stops the container from starting.
