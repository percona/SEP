Script-backed TaskExecutionApp list routes (`GET /`) can opt into offset/limit pagination via `TaskExecutionApp.pagination`, returning a PaginatedResponse envelope; apps that omit it keep the existing unpaginated array response.
