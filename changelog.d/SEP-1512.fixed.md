A task whose `file://` payload cannot be resolved now short-circuits to a terminal FAILED task history on every dispatch path — including the synchronous API, connectivity checks, and chained tasks — instead of surfacing as an HTTP 500 or an endless Celery retry
