JSON REST API for the Checksums plugin under `/api/plugins/checksums/`: `GET /` (list tasks, filterable by status), `GET /{task_name}` (task detail with latest execution status), `POST /` (create task), `DELETE /{task_name}` (delete task), and `GET /schema` (PluginSchema DSL describing fields and list columns for SPA clients)
