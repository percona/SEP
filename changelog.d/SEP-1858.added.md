Inventory API surfaces candidate pairings left by a PMM re-registration, and records a confirmed link as an auditable, reversible identity alias. Candidates are derived on read, so an existing installation sees pairings for splits that predate the upgrade alongside new ones. Each pairing is confirmed on its own: confirming a node does not link its services, which surface as candidates in turn.
