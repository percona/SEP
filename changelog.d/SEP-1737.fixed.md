Celery worker children no longer crash-loop when the database hangs during the settings-override seed at process start; the seed is bounded by a fraction of the prefork liveness deadline, the periodic refresher still starts, and the child runs with env-only overrides until a later refresh cycle lands.
