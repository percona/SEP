Hold Celery beat back until the HTTP API answers its health probe, so an overdue periodic task that calls SEP's own API no longer fails on connect right after a restart. Applies to both topologies that start beat: `python -m app.main --start-celery`, and the PMM side-car, where supervisord ordered beat behind the three API programs without waiting for any of them to listen.
