Receiver response bodies from every delivery-plan request -- the probe, the case search, each resolution step and the bundle upload -- are now withheld from the transport debug log, on both the success and the non-JSON failure path, closing a leak where customer data a plan's receiver returned could reach a log any PMM Extensions administrator can read.
