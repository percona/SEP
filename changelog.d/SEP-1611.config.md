Configurable delivery plan (SEP__DIAGNOSTICS_DELIVERY__*) for diagnostic bundle upload: named secrets, linear HTTP resolution steps, and a terminal multipart upload step. Set via environment or YAML only.
