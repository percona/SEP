The PMM-embedded side-car reads mounted secret files: settings-env.sh consults SECRETS_DIR for SECRET_KEY and every canonical name it derives, leaving a mounted name unexported so the settings classes resolve it from the file.
