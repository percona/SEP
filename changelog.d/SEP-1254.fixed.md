Executor host lookups against a host-only Nomad endpoint no longer fail with "Executor backend unreachable: Expecting value" (the endpoint's trailing slash is now stripped before the Nomad API call).
