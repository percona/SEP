Stale-task alert summaries now read "skipped as stale (executor placement delayed past threshold)" instead of naming Nomad, matching the executor-neutral vocabulary of the tasks service. This release also renames three internal log-cursor columns on the tasks database in place, so rolling the image back to a build from before this release requires `alembic --name tasks downgrade a19da5cf0bca` first.
