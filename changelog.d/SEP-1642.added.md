Task Manager list APIs (tasks, task history, and per-task history) now accept sort and search query parameters — sort by allowlisted columns (default -created_at) and search over task name/owner or history executed_by.
