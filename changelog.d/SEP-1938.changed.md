Diagnosing the side-car image from inside the container no longer offers apt, dpkg, tput, clear or the terminfo database: the layer that clears the CRITICAL findings purges perl-base, which debconf needs, so in-container package installation is permanently unavailable and container logs are the supported diagnostic path.
