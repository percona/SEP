The Alters, PostgreSQL backup, MongoDB backup, and MySQL backup task APIs now expose the shared `anonymize_mask`, `anonymized_entities`, `service_type`, and `connectivity_warning` fields on their list and detail responses, matching the other task apps.
