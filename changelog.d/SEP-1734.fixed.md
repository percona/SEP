A runtime override that changes the PMM, Inventory, or Tasks endpoint now evicts the cached client on the endpoint no longer in use, instead of leaving it open until process shutdown. In Celery workers only the PMM endpoint is covered; a worker's cached Inventory or Tasks client still lingers until shutdown.
