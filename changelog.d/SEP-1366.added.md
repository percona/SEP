Add `one_of` form-group primitive to the plugin schema framework: segmented either/or UI in `SchemaFormRenderer`, branch validation via the rules engine, and model-first derivation from discriminated unions. Additive only — existing plugin GET `/schema` payloads unchanged until a plugin adopts the group.
