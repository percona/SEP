MongoDB server build info snippet (`mongodb_server_build_info.sh`) that captures `db.serverBuildInfo()` from a MongoDB instance — server version and git revision, OpenSSL build, memory allocator, JavaScript engine, enabled modules and build environment — for support diagnostics.
