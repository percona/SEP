MyDumper restores now reject a request with no destination database service, or one whose service resolves to no network address, with a validation error. A destination service recorded without a port now restores to that service's host on the default port 3306, instead of silently falling back to the MySQL instance running on the execution host.
