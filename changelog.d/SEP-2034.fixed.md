MySQL Backups now rejects a create form submitted without a Backup directory, instead of dispatching a task that dies on the execution host. Backup tasks saved earlier without one still open in the edit form, but must be given a directory before they can be saved again.
