Inventory list pages (nodes, services, schemas, and tables) now sort and search on the server across the full result set, so ordering and filtering apply beyond the current page and paginated totals stay accurate under search.
