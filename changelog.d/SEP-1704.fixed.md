The node and service system-observation read endpoints now distinguish their two 404 conditions: a node or service that exists but has no system facts collected yet answers `System observation not collected yet for this node`/`... for this service`, while a node or service that does not exist keeps the default `Not Found`. Both cases previously returned an identical body, so callers and log readers could not tell a healthy uncollected resource from a missing one.
