Add SEP.DATABASE.POOL_PRE_PING (and the matching INVENTORY.DATABASE / TASKS.DATABASE keys) plus CELERY.beat_engine_options.pool_pre_ping, all defaulting to enabled; set any to false to restore the previous checkout behaviour on that engine.
