The MySQL Backups JSON create/update body now accepts only the lowercase field names advertised by `GET /schema` (`backup_type`, `upload`, …); the uppercase aliases the old model exposed (`BACKUP_TYPE`, `UPLOAD`, …) are no longer accepted.
