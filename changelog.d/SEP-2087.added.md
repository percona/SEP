The diagnostics incident list now reports each incident's total run count, failed run count, and last activity time. On an upgraded instance the failed count reads 0 for runs that finished before this release until the reconciliation sweep has caught up; a fresh install has nothing to reconcile.
