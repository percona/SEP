Archives plugin now supports remote `pt-archiver` destinations: pick a destination MySQL service from inventory or enter a host/port/database manually, and the detail page displays the resolved destination. Existing same-server tasks are unaffected.
