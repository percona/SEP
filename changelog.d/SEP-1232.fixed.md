Schema Change (alters) Recursion method dropdown now offers `hosts` instead of the invalid `host`, so `pt-online-schema-change` accepts the value and discovers replicas correctly. Legacy tasks saved with the previous value are normalised on edit.
