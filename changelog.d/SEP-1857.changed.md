Inventory entities that stop being reported by their source are now retired rather than deleted, so a MySQL backup catalog and the task history that reference them keep resolving; retired entities are hidden from every normal read and returned only when a request passes `include_retired=true`.
