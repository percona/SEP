Task history responses now report why a run failed — the failing step and its exit code for Nomad runs, the exception type and callable path for Celery runs, and canned prose for runs that were lost, skipped as stale, or could not be launched. Runs recorded before this release, and runs that fail through a path this change does not cover, report no reason: read a missing reason as unknown, not as did not fail.
