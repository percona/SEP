The XtraBackup restore "Restore my.cnf" option is now marked as destructive in the app schema, naming that it overwrites the target's live configuration files without keeping a copy of what they held.
