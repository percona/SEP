Conditional field gates (`forbidden`/`requires`) and section-level rule evaluation (`cardinality_rules`, `fail_when`) in `<SchemaFormRenderer>`: fields are shown/hidden and marked required based on live form values evaluated against the BE predicate DSL; cardinality and fail_when violations render as inline Alert banners per section and block submission.
