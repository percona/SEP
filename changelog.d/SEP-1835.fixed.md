A stop request on a run that has already finished no longer relabels its outcome: the derived status (for example a payload failure) and the real finish time are kept, and a single terminal annotation is sent to PMM. Task history rows already mislabelled STOPPED by this defect are not backfilled.
