Snippets list sorting and searching now run through the shared core list-query framework, with deterministic ordering across pages; the approval, service-type and uncategorized filters and the service-types facet are unchanged.
