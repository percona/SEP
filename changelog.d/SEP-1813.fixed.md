Alembic upgrades on the sep track also succeed when a stripped app's configured versions directory is still present but empty of migration scripts, not only when that directory is absent from disk — so an app-restricted image can upgrade a database a full image migrated even if the strip left an empty versions/ scaffold behind.
