Support diagnostics runs are now dispatched through an ATW-owned proxy task. Three consequences are visible: the task name reported for a batch execution changes; newly dispatched diagnostics runs no longer appear in the Snippets per-script execution history pane (they remain on the incident's own executions view); and a diagnostics run's task-history display label is now the proxy's name rather than a snippet-and-target label.
