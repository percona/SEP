TaskHistory responses from the tasks service rename ``log_allocation_epoch`` to ``log_producer_epoch``. External consumers of ``POST /history/`` (and any other endpoint that returns ``TaskHistory``) must read the new field name; the value and semantics are unchanged.
