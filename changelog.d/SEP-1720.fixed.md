DB-backed runtime overrides for settings classes owned by the main PMM Extensions app (SEPSettings, SnippetsSettings, MessagesSettings, the global Settings, AlertSettings, and app-owned classes) now take effect in the Celery worker without a restart, as they already did for the Tasks-side classes. Worker-side refresh advances while tasks run rather than on a wall-clock interval, so an override lands on the next task the worker executes.
