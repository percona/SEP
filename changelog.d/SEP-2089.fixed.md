A diagnostic run that fails because of a value entered in the run form now names the responsible parameter and reports the tool's own error, instead of replacing it with prose that omits the field, blames the wrong one, or reports an affirmative finding for a probe that never ran.
