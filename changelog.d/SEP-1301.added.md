Inventory API now exposes system-observation endpoints for nodes (`GET/PUT /api/inventory/nodes/{id}/system-observation`) and services (`GET/PUT /api/inventory/services/{id}/system-observation`), enabling callers to retrieve or upsert a snapshot of host/service-level metrics collected by PMM.
