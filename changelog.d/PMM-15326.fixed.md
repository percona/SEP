OpenManager inventory probes now survive a duplicate dispatch of their own making - adopting an identical in-flight task, and waiting out a dispatch lock left behind by a refused attempt - so a run no longer reports PARTIAL for a probe that succeeded or was never really blocked.
