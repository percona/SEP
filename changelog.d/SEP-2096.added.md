The Support diagnostics run form warns, without blocking submission, when the chosen executor host cannot run sudo for a selected script. It warns only for hosts whose sudo capability a host-facts sync has measured, so a host never measured shows no warning.
