Task history file download: a built-in "Download files" action on `TaskHistoryTable` opens a dialog listing per-run output files and directories, with authenticated per-file (or directory-as-archive) downloads via new `useTaskHistoryFiles` and `useTaskFileDownload` hooks.
