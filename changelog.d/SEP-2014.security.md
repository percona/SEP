Settings-override values typed as credential-bearing URLs now store their embedded password encrypted at rest, by one migration per Alembic track; the surrounding endpoint stays readable. Rolling the code back to an earlier release requires `alembic --name <sep|inventory|tasks> downgrade -1` on each track first, because that release reads the stored password as plaintext and would present the ciphertext to the remote as a credential.
