Upgrade pydantic to 2.13 and sqlmodel to 0.0.42. Importing the side-car's API takes about a fifth of the memory it did (1,025 MiB to 208 MiB resident). The published OpenAPI documents change shape with no change to routes or validation: single-value `Literal` fields no longer repeat their value as a one-item `enum` beside `const`, single-member enums drop their `const`, dict schemas carry `propertyNames`, free-form `dict` schemas state `additionalProperties: true`, and discriminated unions declare a `discriminator` with its `propertyName` and `mapping`.
