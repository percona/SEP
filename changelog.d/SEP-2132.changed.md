Upgrade pydantic to 2.13 and sqlmodel to 0.0.42. Importing the side-car's API takes about a fifth of the memory it did (1,025 MB to 208 MB resident). The published OpenAPI documents change shape with no change to routes or validation: single-value `Literal` fields no longer repeat their value as a one-item `enum` beside `const`, dict schemas carry `propertyNames`, and discriminated unions carry a `mapping`.
