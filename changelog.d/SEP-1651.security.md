Task-log PII anonymization now operates on complete lines instead of individual fetched chunks. A PII token that straddled a Nomad frame, sync-cycle, or live-stream boundary previously escaped redaction and was persisted and displayed verbatim; the trailing partial line is now withheld until it is completed, and flushed on stream end so nothing is dropped.
