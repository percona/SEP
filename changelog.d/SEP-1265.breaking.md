Offset/limit pagination is now centralized in `app.core.pagination`: `limit=0` no longer means "fetch all", list endpoints require `limit>=1`, and integrations/tests must send explicit bounded pagination (`offset`, `limit`) and iterate pages when full traversal is needed.
