`percona/percona-sep:X` is now the frontend-less, app-restricted PMM-embedded image. Every release tag published before this release remains the standalone product, and `:latest` flips to the embedded image at the next stable semver release. v0.13.1 is the final standalone release; the `sep_installer.sh` installer and the `-sidecar`/`-embedded` variant tags are retired.
