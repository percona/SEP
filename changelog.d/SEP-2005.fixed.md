Task-history rows now show usernames instead of raw user IDs under Executed By, Created by and Last updated by, on both the task-history list and the task detail page. The auth provider's user listing is cached for 300 seconds, so a newly created user still renders as a raw identifier until that entry expires, and an actor the provider cannot resolve keeps showing its stored ID.
