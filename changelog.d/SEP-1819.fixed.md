Stop a Nomad allocation with no task states from wedging a task history in RUNNING: the sync no longer fails on that shape, a dead-end allocation reaches a terminal status, and stopping such a task succeeds instead of returning 500.
