MySQL XtraBackup restores with no saved backup binary now use xtrabackup, matching the backup default. Existing restores that omitted the binary use the new default; select innobackupex explicitly to keep using it.
