The task detail API now documents execution history as a typed envelope with permissive row shapes, instead of an arbitrary object, while still passing through unknown upstream keys and resolving actor fields the same way as before.
