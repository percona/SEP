ATW free-text snippet search now works on deployments that do not activate the Snippet Manager app, such as the PMM-embedded side-car profile. A snippet whose frontmatter leaves `title` or `description` empty no longer renders a blank label or fails the ATW snippet listing.
