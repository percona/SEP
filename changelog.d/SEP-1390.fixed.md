Route all Valkey Sentinel collector queries through one configurable Sentinel port (-P, default 26379, validated to the 1-65535 range) so master discovery no longer diverges from the other Sentinel queries on non-default ports.
