A PMM inventory entity is now retired only after two consecutive complete sync generations report it missing, so a removal takes effect one sync interval later than before; entities labelled `sep_sync: disabled` are held indefinitely instead of being deleted, and must be removed explicitly.
