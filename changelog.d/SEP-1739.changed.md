The `make backfill-legacy-forms` command now backfills exactly the apps listed under `APPS` in `settings.yaml` rather than a fixed set: it runs in app-restricted deployments, where a stripped app package previously stopped it from starting, and it no longer backfills an app whose package is installed but whose `settings.yaml` entry was removed.
