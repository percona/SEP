The `make backfill-legacy-forms` migration now backfills exactly the apps activated in `settings.yaml`, so it also runs in app-restricted deployments where a stripped app package previously stopped it from starting.
