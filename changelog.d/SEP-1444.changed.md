Alters list and detail task responses no longer carry the always-null `connectivity_warning` and `pre_checks_auto_fire_warning` fields; these now appear only on the create (both) and update (`connectivity_warning` only) responses.
