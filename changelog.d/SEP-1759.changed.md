GET /periodic/ and GET /api/sep/periodic-tasks/ now return a PaginatedResponse envelope (offset/limit); server-rendered task views and React scheduled-tasks panels walk all pages so schedules are not truncated at the page size.
