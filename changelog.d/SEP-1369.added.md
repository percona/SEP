Admin config export (`GET /api/extensions/admin/settings/export`) accepts repeatable `keys` selectors (`Class` or `Class.KEY`) to export a subset of settings; omitting `keys` still exports the full config.
