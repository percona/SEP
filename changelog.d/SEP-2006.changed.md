A cron expression the scheduler cannot run is now rejected with a 422 naming the offending field, instead of being accepted and failing later when the schedule is saved or read. A malformed cron in the health-report schedule configuration is likewise rejected when settings are read, rather than when the schedule is seeded.
