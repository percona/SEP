The MySQL Restores create form now opens its backup-specific fields with Destination Database Service, ahead of the restore method, and marks it as required while Mydumper is selected, so a Mydumper restore naming no service is rejected at body validation with a message naming the field. XtraBackup and Binlog restores still accept no destination service. The field's helper text now says it populates Backup Source and the Mydumper target database.
