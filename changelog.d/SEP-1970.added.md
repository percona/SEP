The side-car image now ships clear_sentinels.sh, and sidecar/README.md documents the supported way to re-run a schema step inside a running container: clear the step's sentinel with it, then restart the one-shot together with the API programs, so no restarted API can start on the previous run's marker. Container starts and restarts are unchanged; they already clear every sentinel.
