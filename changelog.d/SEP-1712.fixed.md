The "Check connectivity" checkbox is now unchecked by default in deployments whose settings profile omits `CONNECTIVITY_CHECK_DEFAULT`, including the PMM side-car. Operators who want it pre-checked can opt back in via the profile key, `EXTENSIONS__CONNECTIVITY_CHECK_DEFAULT=true`, or a settings override.
