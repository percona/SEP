Migrate deprecated config to its new homes: move PMM connection/auth (`endpoint`, `frontend`, `api_key`, `verify_ssl`, `execution_target`) from `EXTENSIONS.PMM` to the top-level `PMM` section; move the alerts fields (`backup_interval`, `backup_retention`, `alert_folder_name`) from `EXTENSIONS.PMM` to the new alerts-owned `EXTENSIONS.ALERTS` section (`BACKUP_INTERVAL`, `BACKUP_RETENTION`, `ALERT_FOLDER_NAME`); drop any per-syncer `pmm:` override under `SYNCERS` / `SYNCER_EXTRA_KWARGS`; and change plugin `MODULE_NAME` values `backup`/`backups` to `mysql_backups`. Stale `EXTENSIONS.PMM` / `EXTENSIONS__PMM__*` / per-syncer `pmm:` keys are now rejected at startup, so settings must be migrated before upgrading.
