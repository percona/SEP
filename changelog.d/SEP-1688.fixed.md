Database credentials containing URL-reserved characters (notably `/` and `%`) are now percent-encoded when the connection DSN is built, so a password such as `p@ss:w/rd` no longer yields an invalid URL that aborts service startup and migrations.
