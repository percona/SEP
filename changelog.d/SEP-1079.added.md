Migrate archives plugin to React via schema-driven framework; lift cross-field validators to declarative DSL. Whitespace-only values in source/destination name fields (e.g., `source_db_name`, `dest_table_name`) are now treated as present and will trigger cross-field validation conflicts with the corresponding ID field.
