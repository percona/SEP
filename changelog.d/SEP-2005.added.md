Task-plugin list columns and detail fields can carry per-value display labels, so an enum-valued field renders human-readable text instead of its stored code. The backup apps' "Type" surfaces use it; a client that ignores the new field sees an unchanged schema.
