#!/usr/bin/env bash
# ---
# title: << display_name >> sample
# description: Replace with a real script; declare each parameter in the frontmatter.
# parameters:
#   - name: message
#     label: Message
#     positional: true
# ---
echo "Running << name >> with message: ${1:-}"
