#!/usr/bin/env bash
# Sample << display_name >> script. Replace with a real script; the execution
# arguments it accepts are declared on the get_execution_model() form in models.py.
echo "Running << name >> with message: ${1:-}"
